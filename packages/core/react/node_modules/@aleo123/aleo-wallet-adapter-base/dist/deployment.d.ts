export interface AleoDeployment {
    address: string;
    chainId: string;
    program: string;
    fee: number;
}
export declare class Deployment implements AleoDeployment {
    address: string;
    chainId: string;
    program: string;
    fee: number;
    constructor(address: string, chainId: string, program: string, fee: number);
}
