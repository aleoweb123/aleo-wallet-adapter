export interface AleoTransition {
    program: string;
    functionName: string;
    inputs: any[];
}
export declare class Transition implements AleoTransition {
    program: string;
    functionName: string;
    inputs: any[];
    constructor(program: string, functionName: string, inputs: any[]);
}
export interface AleoTransaction {
    address: string;
    chainId: string;
    transitions: AleoTransition[];
    fee: number;
}
export declare class Transaction implements AleoTransaction {
    address: string;
    chainId: string;
    transitions: AleoTransition[];
    fee: number;
    constructor(address: string, chainId: string, transitions: AleoTransition[], fee: number);
    static createTransaction(address: string, chainId: string, program: string, functionName: string, inputs: any[], fee: number): Transaction;
}
